%article
  .container
    .row
      %header
        %h3= @category.name
    - @category.videos.each do |v|
      .video.col-sm-2
        = link_to v, nil do
          %img(src="#{v.small_cover_url}")
        