 
/ %img(src="/tmp/#{@videos.first.small_cover_url}")

/ %img(src="/tmp/S_#{['ff6', 'mig', 'ff'].sample}.jpg")


%article.video_category
  - 4.times do
    %header
      %h3= link_to "#{['Action Movie', 'TV Commedies', 'TV Dramas','Reality TV'].sample}"
    .videos.row
      - 6.times do
        .video.col-sm-2
          = link_to "", nil do
            %img(src="/tmp/#{@videos.sample.small_cover_url}")