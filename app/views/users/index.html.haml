%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - current_user.followed_users.each do |followed|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest('knwang@gmail.com'.downcase)}?s=40")
            = link_to "#{followed.full_name}", user_path(followed)
          %td.extra-padding 
            = "#{followed.queue_items.count}"
          %td.extra-padding 
            = "#{followed.followers.count}"
          %td.extra-padding
            = link_to relationship_path(followed), method: :delete do
              %i.glyphicon.glyphicon-remove