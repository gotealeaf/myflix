.videos.row
  - cat.recent_videos.each do |v|
    .video.col-sm-2
      = link_to image_tag("#{v.small_cover_url}", height: '236', width: '166'), video_path(v)
      %div
        = "Reviews #{v.reviews.count}"