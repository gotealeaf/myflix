%article.video_category
  - @categories.each do |cat|
    %header
      %h3= link_to cat.category, category_path(cat)
    .videos.row
      - cat.recent_videos.each do |video|
        .video.col-sm-2
          = link_to video do
            %img(src="#{video.small_cover_url}")