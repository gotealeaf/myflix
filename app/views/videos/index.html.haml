- @videos.each_slice(5) do |slice|
  %article.video_category
    %header
      %h3= link_to "TV Comedies"
    .videos.row
      - slice.each do |v|
        .video.span2
          = link_to image_tag(v.thumbnail), video_path(v.id)
    