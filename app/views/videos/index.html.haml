- @category_videos.each do |category_name, videos|
  %article.video_category
    %header
      %h3= link_to category_name
    .videos.row
      - videos.each do |video|
        .video.col-sm-2
          = link_to video_path(video) do
            %img{:src => video.small_cover_url}
