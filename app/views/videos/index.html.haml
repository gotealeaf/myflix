%article.video_category
  %header
    %h3
      = link_to "#{@comedy_obj.name}", category_path(@comedy_obj)
  .videos.row
    - @comedy.limit(6).each do |a|
      .video.col-sm-2
        = link_to image_tag("#{a.small_cover}"), video_path(a)
          

%article.video_category
  %header
    %h3
      = link_to "#{@drama_obj.name}", category_path(@drama_obj)
  .videos.row
    - @drama.limit(6).each do |a|
      .video.col-sm-2
        = link_to image_tag("#{a.small_cover}"), video_path(a)

%article.video_category
  %header
    %h3
      = link_to "#{@reality_obj.name}", category_path(@reality_obj)
  .videos.row
    - @reality.limit(6).each do |a|
      .video.col-sm-2
        = link_to image_tag("#{a.small_cover}"), video_path(a)
