%article.video_category
  - @categories.each do |category|
    %header
      .videos.row
        %h3= link_to category.name
        - category.videos.each do |video|
          .video.col-sm-2
            = link_to video, nil do
              %img(src="#{video.small_cover_url}")




%article.video_category
  %header
    %h3= link_to "TV Commedies"
  .videos.row
    - 6.times do
      .video.col-sm-2
        = link_to "", nil do
          %img(src="/tmp/#{['family_guy', 'futurama', 'monk', 'south_park'].sample}.jpg")

%article.video_category
  %header
    %h3= link_to "TV Dramas"
  .videos.row
    - 6.times do
      .video.col-sm-2
        %img(src="/tmp/#{['family_guy', 'futurama', 'monk', 'south_park'].sample}.jpg")

%article.video_category
  %header
    %h3= link_to "Reality TV"
  .videos.row
    - 6.times do
      .video.col-sm-2
        %img(src="/tmp/#{['family_guy', 'futurama', 'monk', 'south_park'].sample}.jpg")
