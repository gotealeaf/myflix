%article.video_category
  - @categories.each do |category| 
    %header
      %h3= link_to category.name, category_path(category)
    =render 'shared/display_videos', videos: category.recent_videos
    
