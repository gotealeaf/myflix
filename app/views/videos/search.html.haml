- unless @nothing_found
  - @results.each do |video|
    .video.col-sm-2
      = link_to video do
        %img(src="#{video.small_cover_url}")
- else
  %h2 Sorry no results were found, please try searching again.