.videos.row
  - @videos.each do |video|
    .video.col-sm-2
      = link_to video do
        %img(src="#{video.small_cover_image_url}")
  = will_paginate @collection, renderer: BootstrapPagination::Rails 
