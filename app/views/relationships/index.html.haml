%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @followed_people.each do |followed_person|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(followed_person.email.downcase)}?s=40")
            = link_to followed_person.full_name, followed_person
          %td.extra-padding
            = followed_person.queue_items.count
          %td.extra-padding 
            = followed_person.followers.count
          %td.extra-padding
            = link_to "", method: :post do
              %i.glyphicon.glyphicon-remove