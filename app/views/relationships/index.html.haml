%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="10%")
        %th(width="5%")
        %th(width="15%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @leaders.each do |leader|
        %tr
          %td
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(leader.email.downcase)}?s=40")
          %td.extra-padding
            = link_to leader.full_name, leader
          %td.extra-padding #{leader.videos.count}
          %td.extra-padding #{leader.followers.count}
          %td.extra-padding
            = link_to relationship_path(leader.id), method: :delete do
              %i.glyphicon.glyphicon-remove
