%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @leads.each do |lead|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(lead.leader.email.downcase)}?s=40")
            = link_to lead.leader.full_name, lead.leader
          %td.extra-padding 
            = lead.leader.queue_items.count
          %td.extra-padding 
            = lead.leader.followers.count
          %td.extra-padding
            = link_to relationship_path(lead), method: :delete do
              %i.glyphicon.glyphicon-remove
