%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @relationships.each do |relationship|
        %tr
          %td
            %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest(relationship.user.email.downcase)}?s=40")
            = link_to relationship.user.full_name, relationship.user
          %td.extra-padding
            = relationship.user.queue_items.count
          %td.extra-padding 
            = relationship.user.followers.count
          %td.extra-padding
            = link_to relationship, method: :delete do
              %i.glyphicon.glyphicon-remove