%section#top-header.row
  %h1.col-md-2
    = link_to 'My Flix', root_path 
  - if logged_in?  
    %ul.col-md-3.clearfix
      %li= link_to "Videos", home_path
      %li= link_to "My Queue"
      %li= link_to "People"
    
    
    .col-md-5.navbar-form
      .form-group
        = form_tag search_videos_path, method: :get do
          .control-group
            = text_field_tag 'search_term', nil, placeholder: 'Search'
            = submit_tag 'Search now', class: 'btn'  


    #user_links.pull-right
      %ul
        %li.dropdown
          %a(href="#" id="dlabel" role="button" data-toggle="dropdown" class="dropdown-toggle" data-target="#")
            ="Welcome, #{current_user.full_name}"
            %b.caret
          %ul.dropdown-menu(role="menu" aria-labelledby="dlabel")
            %li
              %a(href="#") Account
              %a(href="#") Plan and Billing
              =link_to 'Sign Out', sign_out_path
