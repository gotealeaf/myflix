%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @followees.each do |user|
        %tr
          %td
            .pull-left
              %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest('#{user.email}'.downcase)}?s=40")
              = link_to user.full_name, user
          %td.extra-padding= user.queue_items.count
          %td.extra-padding= user.followers.count
          %td.extra-padding
            = link_to "", method: :post do
              %i.glyphicon.glyphicon-remove