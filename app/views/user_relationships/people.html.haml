%section.people
  %header
    %h2 People I Follow
  %table.table
    %thead
      %tr
        %th(width="30%")
        %th(width="20%") Videos in Queue
        %th(width="20%") Followers
        %th(width="30%") Unfollow
    %tbody
      - @user_relationships.each do |relationship|
        %tr
          %td
            .pull-left
              %img(src="http://www.gravatar.com/avatar/#{Digest::MD5.hexdigest('#{user.email}'.downcase)}?s=40")
              = link_to relationship.followee.full_name, relationship.followee
          %td.extra-padding= relationship.followee.queue_items.count
          %td.extra-padding= relationship.followee.followers.count
          %td.extra-padding
            = link_to relationship, method: :delete do
              %i.glyphicon.glyphicon-remove