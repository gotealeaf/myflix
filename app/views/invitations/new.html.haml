%section.invite.container
  .row
    .col-sm-10.col-sm-offset-1
      = bootstrap_form_for(@invitation, layout: :horizontal, label_col: "col-sm-2", control_col: "col-sm-6", class: "invite") do |f|
        %header
          %h1 Invite a friend to join MyFlix!
        %fieldset
          = f.text_field :recipient_name, label: "Friend's Name"
          = f.email_field :recipient_email, label: "Friend's Email Address"
          = f.text_area :message, placeholder: "Please join this really cool site!", rows: 6, label: "Invitation Message"
        %fieldset.actions.control-group.col-sm-offset-2
          .controls
            = f.submit "Send Invitation", class: "btn btn-default"
