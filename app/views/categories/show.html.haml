%section.genre
  %header.clearfix
    %h1= @category.name
  %article.row
    - @category.videos.each do |v|
      .video.col-md-2
        = link_to video_path(v) do
          %img(src="#{v.small_cover_url}")



        