- @categories.each do |cat|
  %article.video_category
    %header
      %h3= link_to cat.name, cat
    .videos.row
      - cat.videos.each do |video|
        .video.col-sm-2
          = link_to video do
            %img(src="#{video.small_cover_image_url}")
