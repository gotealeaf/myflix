- @comedy.each do |comedy|
  %article.video_category
    %header
      %h3
        = "#{comedy.title}"
    .videos.row
      .video.col-sm-2
        = link_to image_tag("#{comedy.small_cover}"), video_path(comedy)